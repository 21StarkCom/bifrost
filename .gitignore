.superpowers/
.claude/
# Local Codex installs stay ignored; the one native repo-marketplace manifest
# is generated, drift-gated, and intentionally committed.
.agents/*
!.agents/plugins/
.agents/plugins/*
!.agents/plugins/marketplace.json
.codex/
.stark/
/config.toml
.DS_Store
node_modules/
dist/codex/
dist/gemini/
.goreleaser-dist/
# sign-manifest.yml workflow scratch files — never committed
build-manifest.json
build-manifest.json.sig
build-manifest.json.pem
build-manifest.sha256
release-notes.md
*.log
.env
.env.*

# local git worktrees (idun gh pr-open does `git add -A`, which would commit these as gitlinks)
/.worktrees/
