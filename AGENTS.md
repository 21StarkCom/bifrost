# AGENTS.md — bifrost (the stark skills + tools repo)

This repo is `21StarkCom/bifrost`: it absorbed `21StarkCom/stark-skills` wholesale under STARK-8248/8249, and the marketplace apparatus bifrost used to be — the Go engine, `catalog/`, `vendor/`, `dist/`, the signed registry and the publish workflows — was deleted in the same change, leaving the skills and tools as the entire repo.

**This is the Codex / Cursor / non-Claude entry point.** Claude Code reads `CLAUDE.md`, not this file — so the two must never disagree. This one is a **routing index**, deliberately kept small; `CLAUDE.md` is the full reference — an order of magnitude longer — and is the source of truth on any conflict. Codex caps its combined instruction chain at 32 KiB, so do not grow this file toward that limit — add depth to `CLAUDE.md` and a pointer here.

## What This Is

The stark skills + tools fleet:

- **A two-stage development pipeline** — `/stark-author` (human-gated spec+plan) → `/stark-build` (check-gated implementation). No LLM-reviews-LLM loops, per the 2026-07-25 autopsy.
- **PR finding publication** — `/code-review xhigh --fix` is the review; `tools/findings_review_post.ts` publishes its findings on the PR as ONE anchored review.
- **Multi-agent IaC review** — `/stark-terraform-review`, `/stark-terragrunt-review`.
- **The ops/session tier** — session, handover, release, persona, GitHub identity swap.

Claude, Codex and Gemini are all enabled (Gemini → `gemini-3.1-pro-preview`, default auth `oauth`). Vertex project/location resolve at runtime via `tools/vertex_config_lib.ts` — **never hardcoded or committed**. Hierarchical config: global → org → repo.

## Operating Principles

This is a **personal playground**, not production. No customers depend on it; the only user is the author.

- **Branch + PR for everything — no exceptions.** Every change lands on a branch and merges through a PR. **Never commit or push to `main` directly.** "Ship straight to main" means *merge once the PR is green* — it does **not** mean bypass the PR. (This file said "Ship straight to main" with no branch+PR rule until 2026-08-07. That was wrong, and it was wrong only for you — Claude read the correct rule the whole time.)
- **No rollout ceremony.** Skip soaking, gating, smoking, canary and gradual-rollout patterns. Merge once green.
- **Every review's findings get posted on the PR.** Inline where anchored, summary comment otherwise — `tools/findings_review_post.ts` (over `review_post_lib.ts::postReview`) does the posting. Don't drop, downgrade or summarize findings away, and don't merge with open findings unaddressed: fix them, or reply on the thread saying why not.
  - **Except on generated paths, which get a body entry instead of a thread** (STARK-5637). `tools/findings_review_post.ts` reports a finding whose only anchor matches a generated-output glob in the review **body**, carrying the file and line it would have anchored to, plus the glob that matched and a preamble saying why. The list is **resolved per run from the target repo** (STARK-6095), highest layer first: `--generated-paths` > the `generated_paths.repos["O/R"].paths` config entry > the target repo's own `.gitattributes` `linguist-generated=true` rows > `generated_paths.default`. `--add-generated-paths GLOB,...` EXTENDS the resolved list instead of restating it; `--generated-paths` still replaces; `--no-generated-split` (or `generated_paths.enabled: false`) disables. `generated_paths.repos` ships EMPTY (STARK-7536): the one entry it carried, `catalog/**` repo-keyed to bifrost, went when bifrost started declaring its own generated catalog trees (`catalog/standards/**`, `catalog/*/skills/**`, `catalog/*/commands/**` — STARK-7363), since the wildcard was broader than what is generated and also demoted the hand-authored `catalog/*/bundle.yaml`, `catalog/*/mcp/**` and `catalog/*/agents/**`. A catalog glob still must never reach the global default — a hand-written `catalog/` elsewhere must keep its threads. Accepted cost: `bundle.yaml`'s sync-written `version:` bump is no longer demoted, so use `--add-generated-paths 'catalog/*/bundle.yaml'` on a sync-PR run that draws a finding there. The reader takes `linguist-generated` Set only (`=true` or bare) and refuses to guess at negation, `=false` or `[attr]` macros; a slash-less declared pattern stays root-anchored rather than git's basename-at-any-depth, so `web/src/__fixtures__/index.json` keeps its thread. An absent or unreadable `.gitattributes` falls back to the configured default and warns on stderr — it never narrows the list to nothing, because an empty list is a silent disable. Both silent-disable routes are refused rather than parsed: an *empty* value, and a value that is itself a flag (`--generated-paths --dry-run` used to mean the glob list `["--dry-run"]` — matching nothing — while swallowing `--dry-run`, so the review really posted). An assembled body over GitHub's 65,536-char review-body cap no longer refuses (STARK-5637) and no longer loses every finding (the 422 carries no index, so the fallback folds the inline comments into the same body and fails larger): **`postReview` degrades** (STARK-6094). The cap constants live in `review_post_lib.ts` so every caller inherits it; the highest-severity findings that fit stay in the review body, and the rest are posted in full as cross-linked follow-up comments on the same PR. A single finding too large for one comment is **segmented** across consecutive comments that reassemble byte for byte, and a summary that alone blows the cap is **relocated in full** with its head and a pointer left in the body (STARK-6116) — but only when that is genuinely a degrade (STARK-6245): the stub must be shorter than the summary *and* the relocated plan must fit, since the leading summary chunks add footer reserve of their own, so a refused plan is never a relocated one. A body still over the cap with nothing left to move is refused by name (`body_over_cap_without_findings`) before any POST. Nothing is dropped or truncated, the split is deterministic, and a payload under the cap is byte-identical to before. **`/code-review --comment` has no such split** — on a shared publish branch, route findings through this tool instead. Why: on bifrost#264, 34 inline threads on `vendor/**` and `dist/**` held the PR at `BLOCKED` with all five checks green under `required_conversation_resolution` — and `auto/marketplace-sync` is **shared**, so they blocked a *different* session's publish. They were unfixable where posted: a regenerated snapshot of code already merged upstream, hand-edits rejected by the drift gate, tree overwritten by the next sync. The mechanism drops generated files from the `changedFiles` set `postReview` partitions against, so nothing is dropped, downgraded, re-classified or auto-resolved — **only the gating thread is withheld**. Globs match the whole repo-relative path, never gitignore's basename rule, so hand-written fixtures like `web/src/__fixtures__/index.json` keep their threads. **Never auto-resolve someone else's thread**: a finding is dispositioned by its author, not by a publisher who wants to merge. And `required_conversation_resolution` is correct — it stays; bifrost hides it in *classic* protection (`/branches/main/protection`), which `rules/branches/main` does not surface, the exact inverse of this repo. **Those body entries get their own heading, not the out-of-diff one** (STARK-6096): `Finding.body_reason` lets the caller say WHY a finding is in the body — `buildReviewBody` cannot tell a withheld in-diff finding from a genuinely out-of-diff one by file/line — and the renderer groups on it, so a generated-path finding sits under "Findings on generated paths — withheld from inline threads" instead of a heading claiming it was outside the PR's scope. That heading claims **in-diff** no more than out-of-diff, deliberately: a reviewer can report a finding on a generated file the PR never touched, and the per-finding note is what states which case an entry is. Labelled groups render first so the generated-path preamble is not separated from the findings it introduces; findings with no `body_reason` still render byte-identically to before (one group is one heading and one pass whichever end it is emitted from), so every other caller is untouched. Two no-drop rules: an unrecognised `body_reason` degrades to the unlabelled group rather than forming a heading-less group the renderer would skip, and a 422-demoted inline comment loses its label on the way into the body.
  - **A killed `gh` names its cause on both paths** (STARK-371 read path, STARK-6112 posting path). `tools/child_termination_lib.ts::explainTermination` is the one shared helper — cause first, so it survives the callers' 400-char slice. `review_post_lib.ts::ghJsonOnce` treats a signal-killed `gh` as a failure **before** parsing its stdout: a `--paginate` killed between pages would otherwise parse as a clean, truncated 200. A non-zero exit behind a 2xx is refused the same way (a later page died at the transport), and `spawnBounded` listens for `child.stdin` errors so a `gh` killed before draining a large POST body reports its cause instead of crashing the tool on an uncaught EPIPE. The cause lives in `GhError.body` too — `postReview`'s `unpostedReason` reads the body, not the message.
  - **And a hung `gh` is bounded** (STARK-6113): 120 s on both spawn paths, override with `STARK_GH_TIMEOUT_MS` (ms; an unusable value is a hard error, never "unbounded"). Measured, not guessed — an 855-file / 7.1 MB paginate took 6.7 s. SIGKILL, never settling a timed-out call on `close`, and the cleared timer are each load-bearing; see `CLAUDE.md`.
  - **The kill reaches `gh`'s whole process group** (STARK-6131). Both paths spawn through `tools/bounded_spawn_lib.ts::spawnBounded` — `detached`, group SIGKILL on timeout and on `maxBuffer` — so nothing `gh` spawned is orphaned. `detached` takes `gh` out of the terminal's foreground group, so the lib forwards SIGINT/SIGTERM/SIGHUP to every live group and re-raises: Ctrl-C still works. It re-raises only when its listener is the sole one — a tool with its own handler already got that delivery, and re-raising handed it one Ctrl-C twice. Not bought back: `detached` is a new *session*, so a supervisor's group-SIGKILL of the tool no longer reaches `gh`. `runCapturing` and its `RunFn` are async as a result; `spawnSync` can do neither half. **`spawnBounded` also latches ESRCH and listens on the read pipes** (STARK-6245): the group is never signalled once the kernel says it is gone (the leader's `exit` probes it with signal 0, and the latch also untracks so `forward` cannot deliver the same recycled-id signal by the other door), and a `stdout`/`stderr` failure is reported as a termination — `status: null` plus a named cause, never a truncated stdout under a clean exit code. **Since STARK-6735 the claim on a group is keyed by call, not id**: `makeGroupKiller` is the one door (claim + latched signaller + that call's release; `trackGroup`/`releaseGroup` are gone), `forward` shares the per-call latch so a group that empties after the exit probe is signalled once rather than once per Ctrl-C, jury's seats take the same door with an exit probe so an emptied seat leaves the forwarding set at once instead of at `emit`, and `run()`'s last-resort result reports the leader's real exit and only a signal the kernel accepted — never an invented SIGKILL. `spawnBounded`'s timeout result follows the same rule (`signal` is `SIGKILL` only when the kernel accepted it, else the leader's real `signalCode`; `status: null` + `error` still say the bound fired), and jury's skipped ladder still files an `attempted: false` kill report naming why nothing was sent. **`forward` reads its sole-listener check BEFORE it signals**: a latch that trips on the last claim untracks, removing `forward`'s own listener mid-delivery, so read afterwards an embedding tool's handler looked sole and drew the re-raise — the two-deliveries failure above, reintroduced by the latch. See `CLAUDE.md`.
  - **`/stark-jury`'s seats get the same forwarding** (STARK-6135). `jury_dispatch.ts::realRunner` detaches its seats too; it claims each seat's group through `bounded_spawn_lib.ts::makeGroupKiller` (was `trackGroup`/`releaseGroup` until STARK-6735), so Ctrl-C reaches an in-flight seat and its descendants. Jury keeps its own SIGTERM → grace → SIGKILL ladder; only the tracking is shared. The lib refuses group ids `<= 1` (`-0` is the tool's own group, `-1` every process the user owns) through one exported predicate, `isSignallableGroup`, which jury's `killProcessGroup` imports rather than restating. Test-writing trap: a child reporting its pids does **not** mean forwarding is armed — under a parallel suite it can write them before the tool gets from `spawn()` to the claim, and a signal landing there kills the tool by default disposition (so "died by the signal" passes vacuously) and orphans the child. Both files' forwarding tests wait for an armed marker instead; see `CLAUDE.md`.
  - **`agent_dispatch_lib.ts::run()` is the third spawn path and gets both halves** (STARK-6147). It backs `iac_review_lib.ts` and `refactor_planner_provider.ts` — headless claude/codex/gemini, which spawn children and bill while alive — and its timeout used to `child.kill()` the direct child only. It now spawns `detached`, sends its SIGTERM → 5 s → SIGKILL ladder to the **group**, and claims the group through `makeGroupKiller`. On a timed-out settle the group gets one SIGKILL (the leader can die on SIGTERM and close the pipes while a descendant ignoring SIGTERM lives on) and the remaining ladder timers are cleared; a normal close delivers nothing, sparing whatever the child left behind. **ESRCH latches**: unlike `child.kill()`, which is a no-op once the child has exited, `process.kill(-pgid)` signals whoever holds the id now, and clearing the ladder on settle does nothing for a call that has not settled — with an escaped descendant holding stdout, a leader that exited at t=0 drew SIGTERM + 2x SIGKILL at an id freed for the whole timeout. So the first ESRCH ends all signalling, and the leader's `exit` probes the group with signal 0 (delivers nothing) while the id cannot yet have been reused. The latch is the shared `bounded_spawn_lib.ts::makeGroupKiller` (STARK-6377), not a private copy: three copies drifted within a day — STARK-6245 taught `spawnBounded`'s to `releaseGroup` on ESRCH (or `forward` delivers the recycled-id signal by the other door) while both `run()` copies kept the bare latch — so all three now take the killer from one factory, pinned in both trees by "a group the kernel has reclaimed is untracked while run() is still open". Settle paths release through the killer (`killTree.release()`), never by id: an id-keyed release is idempotent per id, and an id the latch already let go may belong to a concurrent call's child by then — releasing it by id would leave that live agent deaf to Ctrl-C. Jury takes the same door since STARK-6735 (exit probe, and its ladder is skipped once `gone`), and `run()`'s last-resort result no longer invents a SIGKILL. Not reached: a descendant that `setsid`s out of the group — `run()` still settles past it (the last-resort pipe teardown). The Codex mirror `runtime-overrides/codex/tools/agent_dispatch_lib.ts` carries the same `run()`, and `agent_dispatch_lib.test.ts` runs every tree test against both composed trees.
  - **Rerunning a post is safe** (STARK-6125). A POST can land and still report `unposted` (killed or timed-out `gh`), so `postReview` checks the PR for this payload's marker **before the first write of any kind** — overflow comments included — and on a hit writes nothing and returns `alreadyPosted: true` (exit 0, said on stderr). A failed check refuses rather than risk the double-post — and an unreadable one (a 2xx that is not a JSON array) counts as failed, never as "nothing there", on the reviews list and the issue-comments list alike. Overflow comments orphaned by a truly failed run are adopted into their slots, not re-posted. The marker's run hash now covers the whole payload plus the head sha (`review_post_lib.ts::computeRunHash`, re-exported by `findings_review_post.ts`); the old first-three-ids hash would have let the skip swallow a different review, so any new caller of `postReview` must derive `runHash` with it.
- **Draft PRs by default.** Every PR-opening path opens a **draft**, so WIP stays out of draft-guarded CI. Test locally, then un-draft to merge. Opt out per-run with `--ready` (alias `--no-draft`). You cannot merge a draft, so the merge paths run `gh pr ready` first — which fires target CI via `ready_for_review` — then wait for green, then squash-merge. Target repos need the skip-draft guard for "no CI on WIP" to hold: `standards/workflows/skip-draft-guard.md`.
  - **But never guard a workflow whose check is REQUIRED** (STARK-357). A guarded job reports `skipped`, GitHub counts that as satisfying the required check, and it looks identical to a pass — so the guard turns "CI did not run" into "CI is green". PR #877 merged that way; the suite never ran against what landed. Nothing repairs it after the fact: a re-run replays the original payload and skips again, and a `workflow_dispatch` run never joins the PR's status rollup. `.github/workflows/ci.yml` is deliberately unguarded, and its `test` job runs the `tools/` suite. The merge paths (`idun gh pr-merge`) refuse a skipped required check, naming it; `--allow-skipped-checks` opts back in for path-filtered checks that skip by design.
  - **`main`'s required checks live in a RULESET — and on bifrost you must read BOTH endpoints, because neither alone is the answer.** `gh api repos/O/R/branches/main/protection` returns `404 Branch not protected` on a rulesets-only repo (stark-skills was one), so auditing required checks by reading `branches/*/protection` concludes every repo in the fleet is wide open; read `repos/O/R/rulesets` (config) or `repos/O/R/rules/branches/main` (what is actually enforced on the ref). Here the gate is repository ruleset **23544063 "Required CI on main"** — `target: branch`, `enforcement: active`, condition `~DEFAULT_BRANCH`, `strict_required_status_checks_policy: false` — requiring four contexts: **`test`**, **`typecheck`**, **`secret scan (tree)`** and **`actionlint`**, all four from `.github/workflows/ci.yml`. It carries exactly one bypass actor, `{actor_id: 5, actor_type: RepositoryRole, bypass_mode: always}` — repository admin, which on this repo is the sole operator — **so the operator CAN merge red.** Never write "no bypass" here; STARK-8166 stripped that claim from the governance docs once it was measured against the live ruleset, and the distinction is the whole difference between "CI decides" and "CI advises the one person who can override it". Unlike stark-skills, bifrost ALSO carries **classic branch protection** on `main`: `enforce_admins: true`, `required_conversation_resolution: true`, `required_linear_history: true`, `allow_force_pushes: false`, `required_approving_review_count: 0`, `require_code_owner_reviews: false` — and **no `required_status_checks` key at all**. So the classic endpoint is not empty here and it is not the whole picture either: the ruleset holds the checks, classic protection holds admin enforcement and conversation resolution. A required context is a check-run NAME (`ci.yml`'s job-level `name:`, or the job key where it has none), never the YAML key, so **renaming a job orphans its requirement** — the merge box waits forever on a context nothing will ever report, and this is the one failure `gh pr checks --required` cannot show you: an unreported context has no `statusCheckRollup` entry, so the command you would reach for to diagnose it silently omits the very check that is blocking. Diff the ruleset's contexts against `ci.yml`'s job names directly, and PUT the ruleset in the same window as the rename. A `PUT` replaces the rule's parameters **wholesale** — read, append, write back, or you silently drop a gate while appearing to add one.
  - **And never require a check whose step carries `continue-on-error: true`** — it reports SUCCESS regardless of the step, so requiring it satisfies the gate unconditionally. `typecheck` was stuck in that loop until STARK-5008 (left out of the ruleset for being advisory, advisory because of the flag) while `node --test` could never catch a type error anyway — Node strips types instead of checking them. Flag dropped, then context required. **`tools/workflow_shape.test.ts`** pins that shape here (`tools/typecheck_gate.test.ts` was stark-skills' and was not carried over): `ci.yml`'s exact four-job set, the check-run context each job reports under — with `test` and `typecheck` required to carry **no** job-level `name:`, so their contexts stay the bare ids the ruleset requires — the absence of `continue-on-error` at **any** depth (job *or* step), of a job-level `if:`/`paths`/`paths-ignore`, and of a path filter on `on:`, plus a per-PR concurrency group with `cancel-in-progress: false`. A **step**-level `if:` is deliberately allowed (`secrets` guards its PR-range scan with one, and a skipped step does not skip the job). A fifth job in `ci.yml` fails the suite until its check is decided. The step runs `npm run typecheck`, not `npx tsc` — headless `npx` downloads a missing package instead of failing.
- **Every PR action uses `gh` as `aryeh-stark`.** This includes review posting and release operations. Review text identifies the model; authentication never changes with model choice. Both the canonical and Codex-mirror dispatch libraries strip ambient credentials from Gemini environments while keeping the process-variable floor (`USER` included); reviewer subprocesses also exclude database connection strings. Review Apps are retired. No workflow in this repo holds an App credential or any write permission any more — `marketplace-sync.yml` went with the marketplace apparatus, and `ci.yml` + `secret-scan.yml` are both `permissions: contents: read`.
  - **`idun user` is the other exception, and it is human-invoked only** (moved out of stark-skills in STARK-2215). It moves `gh` to a relief account when `aryeh-stark`'s rate bucket runs dry — `export GH_TOKEN=$(idun user --swap)`. **No tool, skill or hook may invoke it** — it exports `GH_TOKEN`, which overrides `gh`'s keyring for every later call in that shell, so an automated swap silently re-authors whatever runs next.
- **Language: Go for backend, TypeScript for scripts.** **No new Python.** The repo's tooling is TypeScript-only under `tools/`; the former Python orchestrators and dispatch infra under `scripts/` were migrated out and deleted. If you find a `scripts/*.py` path named in any doc, it is stale — delete the reference, don't recreate the file.
- **Test live.** Local-only verification is not enough. If a flow touches GCP, exercise the real GCP surface.
- **Update docs in the same change.** Any change to behavior, structure, commands, env vars or operations updates the relevant docs — **this file and `CLAUDE.md` both**.
- **GCP worktree scope stays local and narrow.** `tools/gcp_scope.ts install` owns each mapped repo's generated `.envrc` block and the exact `.envrc` row in `.worktreeinclude`; `check` validates both. Preserve other include rows/comments and never add credentials or broad globs. Codex-managed worktrees consume the file; plain Git-worktree helpers must copy the explicitly named safe files themselves.
  - **A built-in Claude worktree needs the copied `.envrc` trusted (STARK-5090).** `claude -w` copies `.worktreeinclude` entries but never `direnv allow`s the copy, and direnv keys its allow-list by path **and** content hash, so a gcp-scoped worktree opens with `.envrc is blocked`. `config/settings.json`'s third `SessionStart` hook grants it, resolving the worktree **root** from the session cwd (a subdirectory session still trusts the root `.envrc`) and only when that root is a real linked git worktree (`.git` is a file) — a directory merely named `.claude/worktrees/<x>` is never auto-trusted. `matcher: startup` keeps it off `resume`/`clear`/`compact`, so an `.envrc` edited mid-session still needs a human `direnv allow`. It arrives via `setup.sh`, but only since STARK-5223: the template used to merge *under* the live `~/.claude/settings.json` with a shallow `jq '.[0] + .[1]'`, so a live file that already had `hooks` never gained a template hook. setup.sh now unions hook entries per event.

## Repo Layout

- `tools/` — **all** TypeScript tooling (170 tracked files): dispatchers, agent utilities, session/state, GitHub transport, skill meta-tooling. The only executable surface, and the subject of `ci`'s required `test` + `typecheck` contexts.
- `skill/` — all skills (`skill/*/SKILL.md`, **25** skills across 25 dirs: 22 `stark-*` plus `agnes`, `gru`, `minion`), served as the seven marketplace plugins by the root manifest's per-plugin `skills:` partition. There is no `skill/evals/` any more.
- `global/` — global config + prompts (`config.json`, `forge_heuristics.json`, `prompts/`)
- `scripts/` — shell helpers + JSON only (`healer_patterns.json`). **No Python lives here any more.**
- `config/` — the operator's own Claude Code assets (11 files): `settings.json`, statusline scripts + hooks, `cmux-autoname.sh`, `output-styles/`, `wif-identities.json`. Installed into `~/.claude` by hand, never shipped by a plugin.
- `runtime-overrides/codex/` — **your tree** (56 tracked files: 28 under `skill/`, 20 under `tools/`, 7 under `standards/`, 1 under `global/`). Complete Codex-only variants plus changed support files; mirrors source-relative paths. **Never** make a canonical Claude file "portable" to satisfy Codex, and never let an override leak back into the canonical tree. Source only: native Codex install is retired (see Status), so nothing imports it today.
- `data/persona/` — persona roster
- `standards/` — org-wide doc templates and workflows
- `.claude-plugin/marketplace.json` — the marketplace manifest: seven plugins, each `"source": "./"` plus its own `skills:` partition. Hand-curated, must stay at the repo root.
- `.github/workflows/` — `ci.yml` (`secret scan (tree)`, `actionlint`, `test`, `typecheck` — all four required) and `secret-scan.yml` (the fleet's secret-scan caller)
- `docs/operations/branch-protection.md` — the one doc that survives: what `main` gates on, and the operator-only APPLY commands for the ruleset

The only `docs/` file here is `docs/operations/branch-protection.md`, and there is no
`org/` tree: the doc layout below is what `/stark-init-docs` scaffolds into a *target*
repo, and `org/evinced/` went with `/stark-review`, the only reader of its overrides (STARK-6098).

## Skills

All skills live in `skill/*/SKILL.md`. Full per-skill detail — arguments, failure modes, the reasons behind each guard — is in `CLAUDE.md § Skills`. This is the index.

**Pipeline (in order)**

| Skill | What it does |
|---|---|
| `/stark-author <intent>` | Stage 1 — spec + plan in one session; the operator decides. Agent does all technical QA and asks only operator-oracle questions, then ends with a dead-simple three-layer intent read-back + plain-language sign-off. Emits `docs/specs/YYYY-MM-DD-<slug>-spec.md` + a `.human.md` operator digest, pins an `accepted-base`, opens a draft spec PR, and stamps that PR onto the bound ticket (`pr_url`, `pr_state=open`). |
| `/stark-build <spec-path>` | Stage 2 — autonomous implementation from an accepted spec. One fresh headless session per task, gated by hooks the agent cannot edit. Abort is a first-class success. |
| `/stark-terraform-review` · `/stark-terragrunt-review` | Multi-agent IaC review over `.tf` / `.hcl`. |
| `/stark-fresh-eyes <doc>` | One-shot zero-context review of a prompt/brief/spec before it ships. |

**Workflow & ops**

| Skill | What it does |
|---|---|
| `idun gh pr-open` · `pr-merge` · `cleanup` · `watch` | The PR lifecycle — **moved to idun** (STARK-2211). Open draft → un-draft + squash-merge on green → sweep branches/worktrees. Not a skill in this repo any more. |
| `/stark-session [start\|end]` | Briefing on start, cleanup on end. |
| `/stark-handover [save\|resume\|status]` | Cross-`/clear` continuity under `~/Code/Handovers/`. |
| `/gru start <epic \| --tickets ...>` | **Gru** drives an epic or ticket list to done, one `/minion` per ticket, over alfred + Hermod. No tooling, no store; rerun `start` to resume. Neither Gru nor the Minion writes a ticket custom field (alfred spec STARK-6093): `worker`/`branch` are alfred's at bind, and `pr_url`/`pr_state`/`review_state` belong to whichever path opens or merges the PR — `idun gh pr-open`/`pr-merge`, and in this repo `copilot_land.ts land` and `$stark-author` Phase 6. No `review_evidence_lib.ts` lives here: STARK-6091 deleted its only intended caller (gru's `verifyCompletion`/`settle`) after the spec pinned its base, so idun's copy is the live one, next to the merger that reads it. Each ticket's repo resolves at launch through `frigg repos get` (the call `hermod ticket --repo` itself makes; exit 3 = unregistered); an unresolvable name falls back to a path derived from a registered record's `path`, and that path must be **proved** with `git -C … rev-parse --show-toplevel` first — hermod silently cuts a bad `--cwd`'s worktree from the caller's own repo and exits 0, so an unproved guess works the ticket in the wrong codebase. Unprovable = the ticket is blocked. Seeding the registry is the operator's. Codex uses `$gru`. `start … --new-tab [--repo <name>]` (STARK-7540) makes the session a launcher: `hermod ticket STARK-<epic> --gru` (STARK-7537, after v0.19.0) for a bare epic, a one-line `--prompt-file` brief for every other form, on the epic's id or — under `--tickets` — `GRU-<first ticket's number>`, never a ticket Gru will work (it would read its own peer row as that ticket's Minion and never launch it); checks the launch id's worktree is free first, prints the ack and stops. `--new-tab` starts a Gru, never resumes one. |
| `/minion` | Runs the shared [worker spine](standards/worker-spine.md) + [stand-down contract](standards/stand-down.md) (with `/agnes`). Own one ticket through the repo spine, fix in-scope gaps in the same PR, file a follow-up for a whole effort, verify live (re-run after `--fix`, output posted as a PR comment since stand-down destroys the scrollback) and name that verification in the report, report `done`/`blocked`/`follow-up` to Gru over Hermod, then stand down on `done` with `hermod poison-pill --json` (no behavior-changing flags — after the merge, the ticket close, a clean tree and the report; it targets the session's recorded cwd, not your shell's, so check that tree, and check unpushed work against `origin/<own branch>`, never `origin/main`, which a squash-merge leaves non-empty forever). The pre-arm pane count is two steps — `hermod whoami --json`, then `hermod panes` under the **literal** `workspaceId` it printed — because `$CMUX_WORKSPACE_ID` goes stale when a tab is moved and a bare `hermod panes` then prints nothing over a real count (STARK-7263); a pasted-id slip is retried once, never a reason to skip the stand-down. A `blocked`/stopping exit does not stand down, and neither does a subagent (it shares `$CMUX_SURFACE_ID` and would kill the parent's tab). Standing down needs no go-ahead, and a hand-invoked Minion stands down exactly like a Gru-launched one (with no leader peer, the `done` line is said in-session; the durable evidence is the PR comment either way) — the scope above is what stays non-negotiable; `armed:true` is the only proof it armed and prints only under `--json` — never fire twice. Inside cmux the spine's step 1 also retitles the worker's own tab by role (STARK-7509) — `MINION (1234)`, `AGNES (1234)`, and `GRU (1234)` from `/gru`'s step 0 — with `hermod rename <bare surface UUID> "<ROLE> (<n>)"`; cosmetic, never a blocker. `/minion [STARK-n] --new-tab [--leader <peer>]` (STARK-7540) launches one via `hermod ticket … --minion`, reading the ticket's own repo before defaulting to the one it stands in (STARK-7595, as `/agnes --new-tab` does): with `--leader` the launcher stops; without it the launcher IS the leader and stays to receive the report and run Gru's step-5 confirm — never a launch nobody hears back from (that is `/agnes --new-tab`). |
| `/agnes STARK-n` | **Agnes** — a Minion with no Gru: the operator launches one tab on one ticket (`hermod ticket STARK-n --repo <repo> --agnes`, or `/agnes [STARK-n] --new-tab` from a live session, which reads the ticket's own repo before defaulting to the one it stands in, runs that launch and stops; `--agnes` needs hermod v0.20.0 or later, so the launcher checks `hermod ticket --help` and without it uses `--prompt-file` with a `/agnes STARK-n` brief) and walks away. Same [worker spine](standards/worker-spine.md) and [stand-down contract](standards/stand-down.md) as `/minion`, minus the leader: refuses a brief naming a leader peer (that is `/minion`), sends no `hermod msg` — the ticket is the only report surface — rebases and reruns its own `idun gh pr-merge` on a refusal since nobody sequences it, and **self-confirms** (`gh pr view --json state,mergeCommit` + `alfred task show`) before standing down. A failed self-confirmation, a blocked exit or a stopping follow-up leaves the tab and worktree alive. Codex uses `$agnes`. |
| `/stark-bury <corpse>` | Retire code into the Náströnd graveyard — a subsystem of a living repo, or a whole repo. Footprint verification, interment PR **before** any deletion, deletion PR, optional sealed dump + table drop. The fleet's only destructive ritual: five non-negotiable laws, operator-gated at every prod mutation. The Codex override is model-discoverable; Claude and Codex variants share the same mutation gates. |
| `/stark-fresh-eyes <doc>` | One-shot zero-context review of a doc before it ships. One dispatch per revision, never a round 2. |
| `/stark-memory [--project <slug>\|--all] [--dry-run] [--apply]` | Keep Claude auto-memory under the load/recall caps: `memory_tidy.ts` measures each `MEMORY.md` + topic file (200 lines/25KB index, 200 lines/4KB per file) and flags cross-repo facts; Claude shortens index lines, splits over-cap files, moves foreign-repo facts to their own memory dir. **Dry-run is the default; `--apply` writes.** |
| `/stark-release [patch\|minor\|major]` | Changelog, tag, GitHub Release. |
| `/stark-persona` | Session character voices. |
| `/stark-refactor-plan [dir]` | Planning-only refactor analysis. Never modifies source. |
| `idun user` | Human-only GitHub identity swap — **moved to idun** (STARK-2215). `export GH_TOKEN=$(idun user --swap)`; `idun user gh seed\|remove\|status`. Not a skill in this repo any more. |
| `/stark-init-docs` | Scaffold dev docs. |

**Claude Code account rotation is not a skill here — run `idun cc`** (the `/stark-cc-user` wrapper was retired in STARK-1697; the engine went native in STARK-1614, idun ADR 0010). One piece stays in this repo and must not be mistaken for dead code: `config/statusline-command.sh` is the **only** writer of `~/.claude/.cc-usage-<account>_<org>`, since the 5h/7d percentages arrive only in the statusline stdin payload — `idun cc limits` / `next --best` have no other source of headroom for an inactive seat. Cross-language wire contract with `idun/src/cc/cc_lib.ts`; see the comment block above the writer, and `CLAUDE.md`. The 5H/7D **render** is a usage bar per window (STARK-2807), filled by the idun daemon's live poll of the current seat and falling back to the payload reading when the daemon has no fresh entry. The payload windows are frozen to the launch seat, so after a mid-session rotation they belong to the rotated-away seat; rather than gate them (the retired `resolve_startseat`/`usage_windows_stale`/`.statusline-procseat-<sid>` machinery — STARK-2652, now deleted), the render reads the idun daemon's live poll of the current seat from `~/.claude/.idun-daemon-state.json` (same `accountUuid:organizationUuid` seat key) and uses it as each bar's value, falling back to the frozen payload only when the daemon has no fresh entry — the live number without a restart. The daemon read is fork-free; its `== *"<seat>": {*` presence guard is load-bearing (a missing seat would leak a different seat's percentages, and one account can hold seats in two orgs). The bar renders dim-empty with `—` only when neither source has a value (a daemon value < 0 is treated as absent). Separate from the snapshot-write's `.statusline-seat-current` marker guard. STARK-2206 once had the writer block also fire `idun daemon send --from-file` to feed the `idun daemon` quota watcher; **STARK-2807 removed that push** (idun >= 0.26.0 dropped the socket + `send` verb — the daemon now polls the usage endpoint itself for the active seat's real figures). The snapshot WRITE stays (it is still the daemon's boot seed and `idun cc limits`' only source); do not re-add a `daemon send`.

**Every skill honors `--help`** — a standalone `--help` / `-h` / `help` token prints purpose + usage + arguments and stops, with no preflight and no phases. Guarded by `skill_smoke_test.test.ts`.

## Status

Skills are edited **here** and they take effect **here**. There is no build step, no generated catalog, no `dist/` tree, no registry, no signed manifest and no sync PR: the file you edit in this repo IS the artifact, and it ships the moment the PR merges. The Go engine, `catalog/`, `vendor/`, `dist/`, `bundles/*.json`, `index.json`, `VERSION`, `schema/` and the sign/publish/watchdog workflows were deleted under STARK-8249, and with them `stark sync`, `stark build --check`, `check-bumps` and the coverage gate. Nothing regenerates anything, nothing is vendored anywhere, and no workflow left in this repo holds any write permission (`ci.yml` and `secret-scan.yml` are both `permissions: contents: read`).

- **Native Codex install is RETIRED.** There is no `codex plugin marketplace add 21StarkCom/bifrost`, no `codex plugin add <bundle>@bifrost`, and no `dist/codex-plugins/` package for either to fetch. `runtime-overrides/codex/` — your tree — survives as **source**: the complete Codex-only variants of skills, tools and standards, kept because they are the authored text. Nothing renders or ships it today, so never read a file's presence there as evidence that it reaches a Codex install, and never make a canonical Claude file "portable" to satisfy Codex instead of writing the override. The invocation convention is unchanged: a skill is called by name from a fresh thread, and `$stark-author --help` (or any skill's `--help` / `-h` / `help`) prints purpose + usage + arguments and stops.
- **The repo serves itself as a Claude Code marketplace.** `.claude-plugin/marketplace.json` at the root IS the manifest `/plugin marketplace add 21StarkCom/bifrost` reads, and it must stay at the root because each entry's `source` resolves relative to it. All seven entries carry `"source": "./"` — the repo root itself — plus a `"skills"` list of `./skill/<name>` paths; the seven lists are a **disjoint partition** of the 25 skills, carried over from the old bundle membership. **`skills:` RESTRICTS, it does not add:** measured on a probe install, `stark-design` reports `Skills (1)` and `stark-ops` `Skills (9)` out of a 25-skill root. That only holds because there is no conventional `skills/` directory to auto-discover, which makes the per-plugin list the sole discovery mechanism — **never rename `skill/` to `skills/`**, and re-measure after a `claude` CLI upgrade: if discovery ever turns out to be additive, all 25 skills load into all 7 plugins with no error anywhere. `claude plugin validate --strict .` is the gate on that file.
- **Bumping an entry's `version` is the ONLY thing that makes an installed Claude Code plugin re-fetch.** Deleting `check-bumps` removed the version *gate*, not the version *field*: `~/.claude/plugins/installed_plugins.json` keys `installPath` by version, so a changed `SKILL.md` under an unchanged entry version never reaches the machine while every check stays green. Touch a skill, bump the `version` of every plugin entry whose `skills` list claims it, in the same PR.
- **What "takes effect here" does and does not mean.** Anything that resolves through this checkout — a direct `node tools/…` run, the `~/.claude` links, a project-local install — is live on save. An *installed* plugin still serves from its own cache, so testing an edit against a real install is still merge → bump → `/plugin update`. What is gone is everything that used to sit in between: no regeneration, no drift gate, no attestation, no signature, no second repo.

## Conventions

- **Docs live with the code** under `docs/`, folder per type — `adr/` (`NNNN-<topic>.md`, immutable: supersede, don't edit), `specs/` (`YYYY-MM-DD-<topic>-spec.md`), `retros/` (`YYYY-MM-DD-<topic>-retro.md`). **That is what `/stark-init-docs` scaffolds into a target repo, guarded by `tools/doc_convention.test.ts` — it is not a layout this repo keeps.** This repo carries no `docs/` tree of its own beyond `docs/operations/branch-protection.md`: every skill is documented by its own `SKILL.md` and its `--help`.
- **There is never a `docs/plans/`.** Since `/stark-author` (2026-08-01) the spec carries the plan — task DAG, done-whens, closing verification command.
- Tier by blast radius: trivial → PR only · feature → spec · architectural → ADR + spec.
- Config is JSON, prompts are markdown. **The per-agent × per-domain review prompt corpus is gone** — it belonged to `/stark-review` and was buried with it (STARK-6098). `global/prompts/` now holds one rubric dir per dispatcher (`iac-review/`, `refactor-planner/`), shared by every agent that runs it.

## Secret scanning

`.github/workflows/secret-scan.yml` is a **hand-maintained, byte-identical copy of a Terraform render** — the one file in `.github/workflows/` that is not this repo's to design. The fleet's secret scan is declared in `21StarkCom/21stark` (`repos/secret_scan.tf`), which writes that caller into every rolled-out repo with `github_repository_file`. bifrost sits in `local.secret_scan_excluded` instead, because `main` here carries `enforce_admins: true` plus required PR reviews and the provider's direct commit is rejected even for an admin token — so the bytes arrive by PR. **Change the template in 21stark and copy the new render; never edit the body here.** Its own header says "MANAGED BY TERRAFORM — a hand edit is reverted by the next apply", and on THIS repo nothing reverts it, which is worse rather than better: an edit here just makes bifrost quietly diverge from the fleet. 21stark's `check "bifrost_runs_the_current_secret_scan_caller"` reads this file over the API on every plan and byte-compares it against the render — it only **warns**, it never fails an apply, and it is now the only thing anywhere that notices a fleet pin bump has not reached bifrost.

- **The `google-oauth-client-secret` rule in `.gitleaks.toml` is load-bearing.** The reusable workflow's self-test asserts that exact rule id fires before it will trust a clean scan, so removing or renaming the rule turns the check RED on a repo with no secrets in it. Carrying the rule is what lets bifrost run the fleet's default caller with no `selftest_rule_id` override — the alternative the rollout uses for the repos whose gitleaks configs were left alone.
- **`secret-scan / secret-scan` reports on every PR but is NOT a required context here.** The blocking secret gate is `ci`'s own `secret scan (tree)` job, which runs `gitleaks dir .` over the whole working tree plus the PR's commit range (a secret added and removed inside one PR still fails it). The two are not redundant and neither dominates the other: the in-repo job is **wider** (whole tree, every PR commit) but weaker in assurance (it downloads its binary with no checksum and runs no self-test), while the fleet caller is narrower (incoming commits only) and better-assured. "Not required" is the state today, not a settled verdict — **STARK-7635 owns the enrol-or-not decision** and is still open. `docs/operations/branch-protection.md` §1 carries the full comparison and is the one place to change it; do not argue the question from this file.
- **Never add that context to the ruleset by hand from here.** Every other repo's requirement is Terraform state in 21stark; a hand-made one is invisible to that tier's audit and gets orphaned by the next fleet pin bump that renames the context's right half, which then blocks every merge on "Expected — waiting for status" until an operator edits the ruleset back out. Enrolment belongs in 21stark, not in `gh api … /rulesets` from here.

## GitHub authentication

Local tools use the operator's existing `gh` login as `aryeh-stark`.
Reviews use `COMMENT` events and identify models in their text.
No review App keys, token minting, or Keychain checks are required.
The separate `stark-meridian-ci` App serves GitHub Actions only.

## Where to go deeper

`CLAUDE.md` in this directory. It carries the per-tool reference (every `tools/*.ts`, what it replaced, its gotchas), the full skill documentation with failure modes, the prompt architecture, and the auth SSOTs. When this file and `CLAUDE.md` disagree, **`CLAUDE.md` wins** — and the disagreement is a bug: fix it in the same PR.
